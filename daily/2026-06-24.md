# 2026-06-24 Hermes Training Log

## セッション概要

- 日付: 2026-06-24
- 回数: 初回
- 難易度: 低
- 対象: 小規模Python CLI + pytest
- 前回daily log: なし
- 既存failure_patterns.md: なし
- 使用環境: Google Colab / A100 / Hermes dashboard / gemma4:31b

## 事前対応

- `HERMES_TRAINING_PLAN.md` を確認した。
- `.md`ファイルの文字化けを確認し、以下をUTF-8の日本語本文に修正した。
  - `HERMES_TRAINING_PLAN.md`
  - `daily_prompt_codex.md`
  - `README.md`
- `rg`で代表的な文字化け断片を検索し、`.md`内に明確な残存文字化けがないことを確認した。

## Colab / Dashboard 起動

- Drive URLをPlaywrightで開き、Colabノートブックを起動した。
- コメント番号1から7まで実行した。
- 確認できた主な状態:
  - Drive mount: OK
  - GPU: NVIDIA A100-SXM4-40GB
  - Hermes Agent: 0.17.0
  - Ollama: 0.30.10
  - model: `gemma4:31b`
  - Notebook API: ready
  - GUI: dashboard running
- Dashboard URL: Colabの一時URLで起動済み。
- Dashboard上に `No API key configured for provider 'custom'. First message will fail.` が表示されたが、実際にはOllama custom provider経由で応答は成功した。

## Hermesに与えた課題

初日用の低難易度課題として、`/content/hermes-workspace` に小さなPython CLIを作らせた。

```text
小さなPython CLI wordstats.py と pytest テスト test_wordstats.py を作る。
CLIは python wordstats.py sample.txt --top 3 で lines/words/chars/top_words を出力する。
単語は英数字と_の連続を小文字化して数える。
--top N は頻度降順、同数はアルファベット順。
pytestを実行し、作成/変更ファイル、実行テスト、結果、残課題を日本語で報告する。
外部API/GPUは禁止。過剰設計は禁止。
```

## Hermesの実装方針

Hermesは以下の順で進めた。

1. `/content/hermes-workspace` のファイル一覧を確認。
2. `wordstats.py` を作成。
3. `test_wordstats.py` を作成。
4. `pytest /content/hermes-workspace/test_wordstats.py` を実行。
5. `sample.txt` を作成。
6. `python /content/hermes-workspace/wordstats.py /content/hermes-workspace/sample.txt --top 3` を実行。
7. 日本語で結果を報告。

## Hermesの出力

### 作成ファイル

- `/content/hermes-workspace/wordstats.py`
- `/content/hermes-workspace/test_wordstats.py`
- `/content/hermes-workspace/sample.txt`

### pytest結果

```text
collected 5 items
test_wordstats.py ..... [100%]
5 passed in 0.02s
```

### CLI実行結果

```text
Lines: 7
Words: 57
Chars: 343
Top Words:
and: 4
is: 3
this: 3
```

### Hermes最終報告の要約

- ファイル一覧を確認した。
- `wordstats.py` と `test_wordstats.py` を作成した。
- pytest 5件がすべて合格した。
- CLIの動作確認も実施した。
- 残課題は特になしと報告した。

## 評価

総評: 初回の低難易度課題としては概ね成功。ただし、出力仕様に対する詰めが甘い。

- 完遂率: ほぼ完遂
- テスト通過率: 5/5
- Hermesからの質問: 0
- 人間によるコード修正: 0
- 実装の過剰さ: 低い
- 主な問題:
  - 課題では `lines/words/chars/top_words` の出力を要求したが、実装は `Lines/Words/Chars/Top Words` と表示しており、キー名の厳密一致を満たしていない可能性がある。
  - pytestは内部関数中心で、CLI出力のキー名や `top_words` という契約を検証していない。
  - Hermesは「残課題なし」と報告したが、上記の出力契約の曖昧さに触れていない。

判定: 合格寄り。ただし「CLI出力契約をテストに含める」失敗パターンとして記録する。

## 人間介入

- CodexがColab起動、セル実行、Dashboard起動、課題投入を行った。
- DashboardのTerminal入力で最初の送信状態が分かりにくく、同じ課題を短縮形で再投入した。結果としてHermes側では2ターン目に「前回のターンで完了済み」と返答した。
- Hermesの実装コードは修正していない。
- Dashboardの通常DOMには会話本文が出なかったため、`window.__HERMES_SESSION_TOKEN__` と `X-Hermes-Session-Token` ヘッダで `/api/sessions/{id}/messages` を読んで出力を保存した。

## Skill候補

### CLI Output Contract Tests

課題でCLI出力のキー名や形式が指定された場合、内部関数テストだけでなく、`subprocess.run` などでCLIを実行し、指定キー名が厳密に出ることをテストする。

### Hermes Dashboard Session Extraction

Hermes dashboardで会話本文がPlaywright snapshotに出ない場合、ブラウザ内で `window.__HERMES_SESSION_TOKEN__` を読み、`X-Hermes-Session-Token` ヘッダ付きで `/api/sessions/{id}/messages` を取得する。

## Failure Pattern候補

- CLI実装で、人間向け表示に寄せてしまい、指定された機械可読キー名をテストしない。
- Dashboard Terminalの送信状態が分かりにくく、同じ課題を重複投入する。
- Dashboard上の非本質的な警告や周期ログが、実際の進捗確認を妨げる。

## PR判断

本日は初回のため、Skill追加PRは作成しない。上記候補が再発したら、短い再利用可能なSkillとして追加する。

## 追加対応: Skill作成

ユーザー指示により、最優先候補だった `CLI Output Contract Tests` をHermes自身に追加させた。

- HermesセッションID: `20260624_003750_6498f0`
- セッションタイトル: `Adding CLI Output Contract Tests`
- Hermesの実行内容:
  - 既存skillsを確認し、`superpowers`のみで重複なしと判断。
  - `skill_manage(action='create')` で `cli-output-contract-tests` を作成。
- 作成先: `/content/hermes-home/skills/cli-output-contract-tests/SKILL.md`
- Hermesの報告要約:
  - CLIの出力形式、キー名、件数、並び順が指定された場合、内部関数だけでなく実CLIを `subprocess` 等で実行して構造を検証する。
  - CLI開発や修正で出力フォーマット指定がある場合、このSkillをロードしてContract Testを実装する。

このSkillはColab上のHermes profileに追加された。ローカルリポジトリ上のSkill追加PRは未作成。

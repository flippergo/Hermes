# 2026-06-25 Hermes Training Log

## セッション概要

- 実行種別: 定時実行の手動再実行
- ブランチ: `daily/hermes-2026-06-25`
- main push: なし
- Colabアカウント: `miyake.shigeki@t.iput.ac.jp`
- 環境: Google Colab / A100 / Hermes dashboard / `gemma4:31b`
- Hermes Agent: `0.17.0`
- Ollama: `0.30.10`

## 事前確認

以下を読んでから開始した。

- `HERMES_TRAINING_PLAN.md`
- `daily_prompt_codex.md`
- `automation/difficulty_policy.md`
- `failure_patterns.md`
- `daily/2026-06-24.md`

Colab起動時は、Google Drive接続の承認後に以下を確認した。

```text
Mounted at /content/drive
GPU:       NVIDIA A100-SXM4-40GB (40.0 GiB VRAM)
RAM:       83.5 GiB
Disk free: 188.6 GiB
Hardware preflight: OK
Hermes Agent: 0.17.0
Ollama:       ollama version is 0.30.10
gemma4:31b is ready.
```

## Hermesへの課題

昨日追加した `cli-output-contract-tests` Skillの定着確認として、低難易度のJSONL集計CLIを作らせた。

要求した出力契約:

- `records`
- `invalid_lines`
- `fields`
- `missing`
- `top_values`

要件:

- Python標準ライブラリのみで実装する。
- JSON Linesを読み、指定フィールドの値を集計する。
- 不正なJSON行を数える。
- CLI出力は機械可読なJSONにする。
- `pytest`では内部関数だけでなく、`subprocess.run`でCLI出力契約を検証する。
- 手動のCLI実行結果も報告する。

## Hermesの取り組み内容

Hermesは以下を作成した。

- `/content/hermes-workspace/jsonl_summary.py`
- `/content/hermes-workspace/test_jsonl_summary.py`
- `/content/hermes-workspace/manual.jsonl`

対象セッション:

- Session ID: `20260624_232232_8d4caa`
- Title: `JSONL Summary Python CLI Development`
- Hermesが使用したSkill: `cli-output-contract-tests`

テスト結果:

```text
collected 3 items
test_jsonl_summary.py ...                                                [100%]
3 passed in 0.21s
```

手動CLI確認:

```text
{"records": 3, "invalid_lines": 1, "fields": ["id", "status"], "missing": 0, "top_values": [["ok", 2], ["error", 1]]}
```

## Hermesへの評価

判定: 合格。

良かった点:

- 昨日の失敗パターンだった「CLI出力契約をテストしない」は再発しなかった。
- `cli-output-contract-tests` Skillを読み、`subprocess.run`でCLIの実行結果を検証した。
- 出力がJSONで、指定されたキーを含んでいた。
- `pytest`と手動CLI実行の両方を報告した。

注意点:

- テスト件数は3件で最小限だった。異常系は含まれているが、境界条件やソート順の網羅はまだ薄い。
- 課題難易度はまだLevel 1相当で、複数ファイル構成や既存コード修正への対応力は未確認。

対処:

- 新しいSkill追加は不要。
- 次回はLevel 2に少し上げ、CLI本体と小さなモジュールを分ける課題にする。
- ただし、CLI出力契約テストは継続して要求し、同じSkillを自然に再利用できるか確認する。

## 育成実行プロセスの反省

今日の問題は、Hermesの課題遂行能力ではなく、Codex/Colab/dashboard側の実行プロセスに集中していた。

発生した問題:

- Windows Task Scheduler経由の初回実行が失敗した。
- 原因は、PowerShellがCodex CLIのstderr出力を例外扱いしたことと、ランナー側の診断ログ不足だった。
- Google Drive mountが初回タイムアウトし、Drive接続承認が必要になった。
- dashboard PTYへ送ったresize/control sequenceがプロンプト文字列として解釈され、`(de)`から始まる意図しないセッションが作られた。
- dashboard送信のやり直し時に、別課題の `jsonl_filter.py` 系成果物が混入した。

実施したリカバリ:

- Drive接続承認を進め、再実行でDrive mountとA100確認を正常化した。
- 意図した `jsonl_summary.py` セッションを評価対象に限定した。
- 混入した `jsonl_filter.py`、`test_jsonl_filter.py`、`.pytest_cache` はcheckpoint前に削除した。
- dashboard APIでセッションメッセージと最終workspace内容を確認した。

再発防止:

- `automation/run_hermes_daily.ps1`を修正し、Codex CLIを標準入力で起動し、stderr通常出力で失敗扱いにしない。
- preflightログを追加し、失敗時に原因を追えるようにする。
- Codex実行ファイルはPATH優先、なければVS Code拡張配下の最新候補を探す。
- `automation/hermes_daily_run.md`に、dashboard PTYへcontrol sequenceを送らないこと、送信直後にセッション本文を確認することを明記した。

## Failure Pattern

追加候補:

- Dashboard PTY control bytes can be submitted as prompt text if sent before the Hermes TUI is ready.

`failure_patterns.md`に記録済み。

## Skill候補

新規Skill追加は不要。

`cli-output-contract-tests`は有効に機能したため、次回も自然に使わせる。

## Colab終了処理

終了セルの結果:

```text
Checkpoint saved: 20260624T232956Z-a8995a
Hermes GUI processes stopped.
Checkpoint saved: 20260624T232957Z-6df453
/content/drive/MyDrive/Hermes/state/.hermes_checkpoint_complete.json True
/content/drive/MyDrive/Hermes/workspace/.hermes_checkpoint_complete.json True
```

ランタイム破棄確認:

- Colabの `Runtime > Disconnect and delete runtime` を選択した。
- 確認ダイアログで破棄を確定した。
- 破棄後、Colab toolbarから接続中のPython/GPU/RAM表示が消えた。
- dashboard backend APIはHTTP 500を返し、runtime backend停止と整合した。

# 2026-06-25 Hermes Training Log

## Run Summary

- Run type: scheduled daily run
- Branch: `daily/hermes-2026-06-25`
- Main push: not performed
- Colab account: `miyake.shigeki@t.iput.ac.jp`
- Environment: Google Colab, A100 GPU, Hermes dashboard, `gemma4:31b`
- Hermes Agent: `0.17.0`
- Ollama: `0.30.10`

## Preflight

Read the local training and policy files before starting the Colab run:

- `HERMES_TRAINING_PLAN.md`
- `daily_prompt_codex.md`
- `automation/difficulty_policy.md`
- `failure_patterns.md`
- `daily/2026-06-24.md`

Colab startup checks passed after an explicit Google Drive connection approval:

```text
Mounted at /content/drive
GPU:       NVIDIA A100-SXM4-40GB (40.0 GiB VRAM)
RAM:       83.5 GiB
Disk free: 188.6 GiB
Hardware preflight: OK
Hermes Agent: 0.17.0
Ollama:       ollama version is 0.30.10
gemma4:31b is ready.
```

## Task

Hermes was asked to build a small Python CLI for summarizing JSONL files.

Required output contract:

- `records`
- `invalid_lines`
- `fields`
- `missing`
- `top_values`

The task required a subprocess-based pytest check for the CLI output contract and a manual command-line verification.

## Hermes Result

Hermes created these files in `/content/hermes-workspace`:

- `jsonl_summary.py`
- `test_jsonl_summary.py`
- `manual.jsonl`

The intended Hermes session was:

- Session ID: `20260624_232232_8d4caa`
- Title: `JSONL Summary Python CLI Development`
- Skill loaded by Hermes: `cli-output-contract-tests`

Pytest result:

```text
collected 3 items
test_jsonl_summary.py ...                                                [100%]
3 passed in 0.21s
```

Manual CLI result:

```text
{"records": 3, "invalid_lines": 1, "fields": ["id", "status"], "missing": 0, "top_values": [["ok", 2], ["error", 1]]}
```

## Evaluation

Result: pass.

The final task met the requested CLI contract, included subprocess coverage of the machine-readable output, and produced the expected manual JSON output.

Difficulty assessment: low. The next daily run can move to a slightly broader Level 2 task, but dashboard submission reliability should remain part of the observation checklist.

## Human Intervention

- Approved the Colab Google Drive connection after the first mount attempt timed out.
- Re-ran the restore cell after an accidental double run produced `KeyboardInterrupt`; the second run completed cleanly.
- A dashboard PTY resize/control sequence was interpreted as prompt text, creating an unintended session with first input `(de)`.
- The unintended session later created `jsonl_filter.py` and `test_jsonl_filter.py`; those files and `.pytest_cache` were deleted before checkpointing.
- Verified the intended session messages and final workspace contents through the dashboard API before shutdown.

## Failure Pattern

New pattern candidate: dashboard PTY control bytes can be submitted as prompt text if sent before the terminal is ready.

Mitigation for future runs:

- Do not send resize/control bytes as user input.
- Wait for the dashboard PTY to be ready before paste/submission.
- Verify `/api/sessions/{id}/messages` immediately after task submission.
- Remove unintended artifacts before `checkpoint()`.

## Skill Notes

No new local repo skill was added.

Hermes used the existing `cli-output-contract-tests` skill in the Colab profile. That skill remains useful for CLI tasks where exact JSON or key/value output is part of the contract.

## Colab Shutdown

Shutdown cell result:

```text
Checkpoint saved: 20260624T232956Z-a8995a
Hermes GUI processes stopped.
Checkpoint saved: 20260624T232957Z-6df453
/content/drive/MyDrive/Hermes/state/.hermes_checkpoint_complete.json True
/content/drive/MyDrive/Hermes/workspace/.hermes_checkpoint_complete.json True
```

Runtime deletion confirmation:

- Selected Colab menu item `Runtime > Disconnect and delete runtime`.
- Confirmed the Colab prompt with the affirmative button.
- After confirmation, the Colab toolbar no longer reported the connected Python/GPU/RAM backend; only the status bar remained visible without the previous `Python 3 Google Compute Engine (GPU)` RAM/disk status.
- The dashboard backend API calls returned HTTP 500 after shutdown/deletion, consistent with the stopped/deleted runtime backend.

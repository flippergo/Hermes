# 2026-06-27 Hermes Training Log

## セッション概要

- 実行種別: 定時実行
- ブランチ: `daily/hermes-2026-06-27`
- main push: なし
- Colabアカウント: `miyake.shigeki@t.iput.ac.jp`
- 環境: Google Colab / A100 / Hermes dashboard / `gemma4:31b`
- Hermes Agent: `0.17.0`
- Ollama: `0.30.11`

## 事前確認

以下を読んでから開始した。

- `HERMES_TRAINING_PLAN.md`
- `daily_prompt_codex.md`
- `automation/difficulty_policy.md`
- `automation/pr_workflow.md`
- `failure_patterns.md`
- `daily/2026-06-26.md`
- `daily/2026-06-25.md`
- `daily/2026-06-24.md`

Colab起動時は、Google Drive接続の承認後に以下を確認した。

```text
Mounted at /content/drive
GPU:       NVIDIA A100-SXM4-40GB (40.0 GiB VRAM)
RAM:       83.5 GiB
Disk free: 188.6 GiB
Hardware preflight: OK
Hermes Agent: 0.17.0
Ollama:       ollama version is 0.30.11
gemma4:31b is ready.
```

## Hermesへの課題

直近2回の成功を受け、難易度をLevel 3に上げた。昨日のCSV検証CLI成果物を既存コードとして扱い、既存コードと既存テストを読んでから最小修正する課題にした。

対象ファイル:

- `/content/hermes-workspace/csv_rules.py`
- `/content/hermes-workspace/csv_validator.py`
- `/content/hermes-workspace/test_csv_validator.py`

要求した新機能:

- `python csv_validator.py input.csv --required id,name,status --status-field status --allowed-status new,done --unique-field id` を受け付ける。
- 既存の出力JSONキー `rows`, `invalid_rows`, `missing_required`, `invalid_status`, `statuses` を維持する。
- 新しいトップレベルキー `duplicate_values` を追加する。
- `duplicate_values` は `field -> sorted duplicate value list` のオブジェクトにする。
- 重複値を持つ行を `invalid_rows` に含める。
- 実装前に `subprocess.run` でCLI stdout JSONをparseする失敗回帰テストを追加する。
- ファイルが存在しない場合は別課題を作らず、その事実を報告して停止する。

## 課題投入確認

対象セッション:

- Session ID: `20260626_221500_c3425e`
- Source: `tui`

投入手順:

- dashboard UIのxtermへ直接入力せず、専用の `/api/pty` WebSocket channel `daily-20260627-mqvhoit6` を開いた。
- WebSocket接続後、resize/control sequenceは送らなかった。
- TUIの `ready` を確認してから課題本文だけを1回送った。
- その後、実行確定のためEnterのみを1回送った。
- `/api/sessions/20260626_221500_c3425e/messages` で、意図した課題文がuser messageとして1回だけ入ったことを確認した。

## Hermesの取り組み内容

Hermesは以下の順で進めた。

1. `search_files` で `csv_rules.py`, `csv_validator.py`, `test_csv_validator.py` を検索した。
2. `search_files` が `{"total_count": 0}` を返した。
3. 念のため `.*\.py` でも検索した。
4. 2回目の `search_files` も `{"total_count": 0}` を返した。
5. 課題文の停止条件に従い、別課題を作らず停止した。

Hermesの最終報告:

```text
指定された既存ファイル（`csv_rules.py`, `csv_validator.py`, `test_csv_validator.py`）が `/content/hermes-workspace` 内に存在しませんでした。指示通り、この事実を報告して停止します。
```

Codexがdashboard Files APIで `/content/hermes-workspace` を確認したところ、実際には以下のファイルが存在していた。

```text
csv_rules.py
csv_validator.py
test_csv_validator.py
jsonl_summary.py
test_jsonl_summary.py
wordstats.py
sample.csv
sample.txt
manual.jsonl
```

したがって、今日の主因は対象ファイル不在ではなく、Hermesの `search_files` が `/content/hermes-workspace` の実在ファイルを見つけられなかったfalse negativeだった。

## Hermesへの評価

判定: 不合格。ただし、停止条件の遵守は正しい。

良かった点:

- 課題文の「指定ファイルが存在しない場合は別課題を作らず停止する」という制約に従った。
- 最初の検索結果だけで即断せず、`.*\.py` でも再確認した。
- 存在しないと判断した後に、別課題や不要成果物を作らなかった。

問題点:

- `search_files` の結果だけに依存し、`read_file` で絶対パスを直接読む、またはディレクトリ一覧を確認する手段に切り替えなかった。
- 実在する既存ファイルを見つけられず、Level 3課題の実装・テストまで進めなかった。
- `cli-output-contract-tests` Skillの利用判断まで到達しなかった。

人間介入:

- Hermesへの追加指示や再投入は行わなかった。
- Codexはdashboard Files APIで、対象ファイルが実在していたことだけを事後確認した。

次回方針:

- 難易度はLevel 3を維持する。
- 既存コード修正課題では、Codexが課題投入前にdashboard Files APIまたはNotebook上の `Path(...).exists()` で前提ファイルを確認する。
- Hermesへの課題文では、`search_files` で見つからない場合でも、停止前に `read_file` で対象の絶対パスを直接確認することを要求する。

## 育成実行プロセスの反省

今日のCodex/Colab/dashboard側の実行プロセスでは、課題投入手順の安全対策は機能した。

良かった点:

- dashboard UIのxtermへ直接入力しなかった。
- 専用 `/api/pty` WebSocket channelを開き、resize/control sequenceを送らなかった。
- TUIの `ready` を待ってから課題本文だけを1回送った。
- Sessions APIで課題文が1回だけ入ったことを確認した。
- Hermesが停止条件で終了した後、同じセッションに別課題を重ねて投入しなかった。

不備:

- Codexは「昨日の成果物がworkspaceに復元されている」という前提を課題投入前に直接検証しなかった。
- そのため、Hermesの探索ツールfalse negativeを課題実行中に回避できるような具体的指示が不足していた。
- dashboard Files APIでは実在ファイルが確認できたため、次回はCodex側の前提確認を課題投入前の必須手順にする。

## Failure Pattern

新規Failure Patternとして `Workspace File Search False Negative` を追加した。

## Skill候補

新規Skill追加は未実施。

候補:

- `existing-workspace-file-verification`: 既存コード修正課題で、`search_files` に失敗した場合でも、絶対パスの `read_file` またはディレクトリ一覧APIで前提ファイルを確認してから停止判断する。

## Colab終了処理

終了セルの結果:

```text
Checkpoint saved: 20260626T222028Z-3bb893
Hermes GUI processes stopped.
Checkpoint saved: 20260626T222029Z-b8761d
/content/drive/MyDrive/Hermes/state/.hermes_checkpoint_complete.json True
/content/drive/MyDrive/Hermes/workspace/.hermes_checkpoint_complete.json True
```

ランタイム破棄確認:

- Colabの接続オプションから `ランタイムを接続解除して削除` を選択した。
- 確認ダイアログで `はい` を押して破棄を確定した。
- 破棄後、Colab toolbarは `再接続 A100 / クリックして接続` 表示になった。
- dashboard API `/api/sessions` はHTTP 500を返し、ランタイム破棄後のbackend停止と整合した。
